{
  "name": "whose-tagline-is-it-anyway",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "babel --presets es2015 src --out-dir build",
    "cover": "istanbul cover _mocha",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "author": "Matthew Doles",
  "license": "MIT",
  "dependencies": {
    "ask-sdk": "2.5.2",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "devDependencies": {
    "alexa-skill-test-framework": "^1.2.4",
    "coveralls": "^3.1.0",
    "istanbul": "^0.4.5",
    "mocha": "^8.2.0",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
